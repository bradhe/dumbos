#pragma once

typedef unsigned int COUNTER;
typedef unsigned char BYTE;

/* Boolean types */
typedef int BOOL;

#define UNUSED(expr) do { (void)(expr); } while (0)

#define NULL    0
#define TRUE    1
#define FALSE   0
